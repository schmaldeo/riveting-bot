[package]
edition = "2021"
name = "riveting-bot"
repository = "https://github.com/Samzyre/riveting-bot"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0"
async-trait = "0.1"
dotenv = "0.15"
futures = "0.3"
htp = "0.4"
indoc = "1.0"
rand = "0.8"
serde = "1.0"
serde_json = "1.0"
thiserror = "1.0"
tracing = "0.1"
twilight-cache-inmemory = "0.12"
twilight-gateway = "0.12"
twilight-http = "0.12"
twilight-mention = "0.12"
twilight-model = "0.12"
twilight-standby = "0.12"
twilight-validate = "0.12"

[dependencies.twilight-util]
features = ["full"]
version = "0.12"

[dependencies.chrono]
features = ["serde"]
version = "0.4"

[dependencies.tracing-subscriber]
features = ["env-filter"]
version = "0.3"

[dependencies.tokio]
features = ["full"]
version = "1.17"

[dependencies.songbird]
default-features = false
features = ["driver", "twilight-rustls", "zlib-stock"]
optional = true
version = "0.2"
# git = "https://github.com/serenity-rs/songbird"
# branch = "next"

[features]
# Default features
default = ["admin", "owner"]
# Debugging features
debug = ["all-intents", "bulk-delete"]
#
admin = []
all-intents = []
bulk-delete = ["admin"]
owner = []
voice = ["dep:songbird"]
